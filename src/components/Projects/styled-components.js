'use client'

import styled from "styled-components"

export const ProjectEntryLink = styled.a`
    width: 100%;
    display: flex;
    flex-direction: column;
    gap: 8px;
    transition: 0.5s;
    padding-right: 4px;
    &:hover {
        transform: scale(1.02);
    }
`
