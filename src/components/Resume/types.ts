

export interface WorkExp {
    company: String,
    title: String,
    startDate: String,
    endDate: String,
    bulletPoints: Array<String>
}

export interface EduExp {
    school: String,
    graduationDate: String,
    degree: String
}